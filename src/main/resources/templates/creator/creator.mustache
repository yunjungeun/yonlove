<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>크리에이터</title>
    {{>layout/header}}
</head>

<body>
{{>layout/sidebar}}
{{>layout/topbar}}

<div class="container">
<h1>크리에이터 현황</h1>

        <table border="1" cellpadding="1" cellspacing="1">
            <ul>
                <tr>
                    <td>채널 ID </td>
                    <td>채널 이름 </td>
                    <td>구독자 수 </td>
                    <td>유저 ID </td>
                </tr>
                {{#selectListCreator}}
                    <tr>
                        <td><a href="/creator/{{ch_id}}/selectcreator">{{ch_id}}</a></td>
                        <td>{{ch_name}}</td>
                        <td>{{ch_sub}}</td>
                        <td>{{user_id}}</td>
                    </tr>
                {{/selectListCreator}}
            </ul>
        </table>
<br/>

    <div class="container1">
        <a href="/creator/insertcreatorview" role="button">작성</a> &nbsp;&nbsp;
        <a href="/creator/creater">목록</a> &nbsp;&nbsp;
        <a href="/index">홈 페이지</a> <br/>
    </div>

<!--페이징 처리 레이아웃-->
{{>layout/paging}}

<br/>
    <div class="container2">
        <form action="/creator/creater" method="GET">
            <select id="searchType" name="searchType">
                <option value="all">선택</option>
                <option value="title">크리에이터 이름</option>

            </select>
            <input type="text" id="search" name="search" placeholder="검색">
            <input type="submit" value="검색">
        </form>
    </div>

</div>
{{>layout/footer}}
<script>
    var url = "/creator/creater";
</script>
<script src="/static/js/search.js"></script>
{{>layout/bootstrapjs}}
</body>
</html>