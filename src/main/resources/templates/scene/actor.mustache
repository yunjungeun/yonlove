<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>출연자 관리 리스트</title>
</head>
<body>
<h1>출연자 게시판</h1>
<table border="1" cellpadding="1" cellspacing="1">
    <ul>
        <tr>
            <td>씬 번호</td>
            <td>출연자 id(조인으로 성명들어가야함)</td>
            <td>출연장 배역/역할</td>
        </tr>
        {{#selectListActor}}
            <tr>
                <td>{{actor_id}}<!--여기누르면 시나리오로 이동해야함--></td>
                <td><a href="/scene/{{actor_id}}/selectactor">{{scene_id}}</a></td>
                <td>{{actor_info}}</td>
            </tr>
        {{/selectListActor}}
    </ul>
</table>

<!--이전 버튼-->
{{#paging}}
    {{#hasPre}}
        <!--{!{검색어유무에따른url}}/{!{실제페이지번호}}-->
        <a href="/scene/{{pageRink}}{{prePage}}">이전</a>
    {{/hasPre}}
{{/paging}}
<!-- 페이지 번호를 반복문으로 생성 -->
{{#pagelist}}
    <!--현재페이지는 링크가 없고 강조표시-->
    {{#currentPageFlag}}
        <strong>{{currentPage}}</strong>
    {{/currentPageFlag}}
    <!--현재 페이지가 아닌 숫자들은 해당페이지로 이동하는 링크생성-->
    {{^currentPageFlag}}
        <a href="/scene/{{pageRink}}{{currentPage}}">{{currentPage}}</a>
    {{/currentPageFlag}}
{{/pagelist}}

<!--다음페이지 버튼-->
{{#paging}}
    {{#hasNext}}
        <a href="/scene/{{pageRink}}{{nextPage}}">다음</a>
    {{/hasNext}}
{{/paging}}

<form action="/scene/actor" method="GET">
    <select id="searchType" name="searchType">
        <option value="all">선택</option>
        <option value="title">actor_id</option>
        <option value="content">역할</option>
        <!-- 필요한 항목 추가 -->
    </select>
    <input type="text" id="search" name="search" placeholder="검색">

    <input type="submit" value="검색">

</form>

근데 작성자는 잇어야되지 않을까요?<br/>
<a href="/scene/insertactorview" role="button">작성</a>
<a href="/scene/actor">목록</a>
<a href="/index">홈 페이지</a>

<script>
    var url = "/scene/actor";
</script>
<script src="/static/js/search.js"></script>

</body>
</html>